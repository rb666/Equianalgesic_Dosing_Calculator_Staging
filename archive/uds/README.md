# Retained UDS source

Temporarily removed from the active site on 2026-09-13. These five application files are retained in Git for later work; they are never copied into the Pages artifact.

Critical interpretation bugs repaired here: multiple possible expected sources no longer depend on entry order; contradictory detected/absent entries are rejected and defensive engine validation blocks conclusions/copying; OUD absence warnings require verified coverage of the matching expected analyte. The generic-screen example no longer pre-attests coverage. Summary text no longer asserts that no identifiers were entered.

Run node --test tests/uds-archive.test.cjs for the real engine's built-in golden cases and added regressions. Tests instrument browser initialization in memory without modifying the engine. Preservation and passing tests are not authorization to restore this tool.

Before restoration, complete the archived audit follow-ups for local-profile storage failures, strict specimen-value parsing and full keyboard/mobile accessibility. Reconnect assets and routes deliberately and perform the complete browser smoke test; keep identifier protections, clinical-use boundaries and local-only persistence. UDS removal from production calc.med remains pending a separately authorized release.
